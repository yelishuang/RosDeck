# RosDeck Architecture (Structure Only)

This document intentionally contains placeholders.
Once the structure is frozen, we will populate diagrams and flows.

## Folders
- `backend/` FastAPI service skeleton (no implementation yet)
- `nginx/`, `scripts/` samples to be added when approved
